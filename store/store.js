//document.addEventListener("DOMContentLoaded", function() {});
/*let totalApagar = 0; // Cambiado de const a let

const cantidad = Number(prompt("¿Cuántos productos desea llevar?"));
    for (let index = 1; index <= cantidad; index++) {
        const nombre = prompt("¿Qué producto desea llevar?");
        const cantidadProducto = Number(prompt("¿Cuántas unidades?"));
        const precio = Number(prompt("¿Cuánto cuesta cada unidad?"));
        const subtotal = cantidadProducto * precio;
        totalApagar = totalApagar + subtotal;
    }
    const total = comprar() + comprar() + comprar();

alert("Total a pagar: " + totalApagar);*/  

//

    /*const comprar = ()=> {
    let totalApagar = 0
    for (let index = 1; index <= cantidad; index++) {
    const nombre =prompt("¿Qué producto desea llevar?");
    const cantidad = Number(prompt("¿Cuántas unidades?"));
    const precio = Number(prompt("¿Cuánto sale cada unidad?"));
    const subtotal = cantidad * precio;
    totalApagar = totalApagar + subtotal;
    }
    console.log(totalApagar);
    return totalApagar;
    }
    const total1 = comprar();
    const total2 = comprar();
    const total3 = comprar();
    const total = total1 + total2 + total3;
    const total = comprar() + comprar() + comprar();*/

//
/*
const comprar = () => {
    let totalApagar = 0;
    const cantidadProductos = Number(prompt("¿Cuántos productos desea llevar?"));
    for (let index = 1; index <= cantidadProductos; index++) {
        const nombre = prompt("¿Qué producto desea llevar?");
        const cantidad = Number(prompt("¿Cuántas unidades?"));
        const precio = Number(prompt("¿Cuánto sale cada unidad?"));
        const subtotal = cantidad * precio;
        totalApagar += subtotal;
    }
    console.log("Total a pagar: " + totalApagar);
    return totalApagar;
}

const total1 = comprar();
const total2 = comprar();
const total3 = comprar();
const total = total1 + total2 + total3;
console.log("Total general: " + total);*/

/*const comprar = () => {
    let totalApagar = 0;
    const cantidad = Number(prompt("¿Cuántos productos desea llevar?"));
    for (let index = 1; index <= cantidad; index++) {
        const nombre = prompt("¿Qué producto desea llevar?");
        const cantidadProducto = Number(prompt("¿Cuántas unidades?"));
        const precio = Number(prompt("¿Cuánto sale cada unidad?"));
        const subtotal = cantidadProducto * precio;
        totalApagar += subtotal;
    }
    console.log("Total a pagar: " + totalApagar);
    return totalApagar;
}

const total = comprar() + comprar() + comprar();
console.log("Total general: " + total);*/



    



    

    
 